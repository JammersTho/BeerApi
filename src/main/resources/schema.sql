CREATE TABLE BEER (
    id INT NOT NULL,
    name VARCHAR(500) NOT NULL,
    tagline VARCHAR(500) NOT NULL,
    first_brewed VARCHAR(500) NOT NULL,
    description VARCHAR(800) NOT NULL,
    image_url VARCHAR(500) NOT NULL,
    abv DECIMAL NOT NULL
);